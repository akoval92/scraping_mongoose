<nav class="navbar navbar-light bg-light">
  <span class="navbar-brand mb-0 h1">Mongo Scraper</span>
</nav>


<div class="card">
  <div class="card-header">Articles</div>
  <div class="card-body">
    <h5 class="card-title">
        {{#each result.title}}
    </h5>
    <p class="card-text">{{#each result.summary}}</p>
    <p class="card-text">{{#each result.link}}</p>
    
  </div>
</div>